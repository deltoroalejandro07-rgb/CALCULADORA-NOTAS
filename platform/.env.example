# Base de datos (Neon/Postgres)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Auth.js
AUTH_SECRET="generate-with-npx-auth-secret"
AUTH_URL="http://localhost:3000"

# Email (verificación de cuenta) - SMTP (Resend, Postmark, Mailtrap en dev, etc.)
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""
EMAIL_FROM="onboarding@tu-dominio.com"

# Stripe
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
# Precio de cada informe mensual (tras el primero, que es gratis), en céntimos
REPORT_PRICE_CENTS="900"

# Anthropic (generación de planes)
ANTHROPIC_API_KEY=""
